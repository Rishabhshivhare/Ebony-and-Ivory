#include <stdio.h>
main(){
	int a,b ,c;
	int cc;	
	scanf("%d%d%d",&a,&b,&c);
	
	
if(a==c && a!=b){

	cc=1*a + 0*b;
	if(a==cc)
		printf("Yes");
	
	
} else 
	{
	int sum;
	sum=1*a + 2*b;
	if(sum==c){
		printf("Yes");
	}
	else printf("No");
} return 0;
}
